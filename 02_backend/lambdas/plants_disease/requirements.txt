# requirements.txt —— for lambdas/plants_disease

# MySQL client (pymysql is pure Python, easy to package)
PyMySQL>=1.0.3

# If you need SQLAlchemy/ORM you can add it, but currently fetch_all with pymysql is sufficient
# SQLAlchemy>=2.0.0

# AWS Lambda runtime already includes boto3 / botocore, no need to add separately
# boto3>=1.35.0

# For debugging or local running
awslambdaric>=2.0.0

# Optional: log beautification
# python-json-logger>=2.0.7
